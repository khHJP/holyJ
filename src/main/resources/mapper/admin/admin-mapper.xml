<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sh.oee.admin.model.dao.AdminDao">

	<!-- select -->
	<select id="selectAdminMemberList" resultMap="adminMap">
		select
			m.*,
			auth
		from
			member m join authority a
			on m.member_id = a.member_id
		where
			delete_date is null
		order by
			enroll_date desc
	</select>
	
	<resultMap type="member" id="adminMap">
		<id column="member_id" property="memberId"/>
		<result column="member_id" property="memberId"></result>
		<result column="password" property="password"></result>
		<result column="nickname" property="nickname"></result>
		<result column="phone" property="phone"></result>
		<result column="manner" property="manner"></result>
		<result column="profile_img" property="profileImg"></result>
		<result column="dong_no" property="dongNo"></result>
		<result column="dong_range" property="dongRange"></result>
		<result column="report_cnt" property="reportCnt"></result>
		<result column="enroll_date" property="enrollDate"></result>
		<result column="delete_date" property="deleteDate"></result>
		<association property="auth" javaType="authority">
			<id column="member_id" property="memberId"/>
			<result column="member_id" property="memberId"/>
			<result column="auth" property="auth"/>
		</association>
	</resultMap>
	
	<select id="selectAdminCraigList" resultMap="craigMap">
		select
			*
		from
			craig
		order by
			reg_date desc
	</select>
	
	<resultMap type="craig" id="craigMap">
		<id column="no"  property="no"/>
		<result column="category_no" property="categoryNo"/>
		<result column="writer" property="writer"/>
		<result column="title" property="title"/>
		<result column="state" property="state"/>
		<result column="reg_date" property="regDate"/>
		<result column="complete_date" property="completeDate"/>
	</resultMap>
	
	<select id="selectAdminLocalList" resultMap="localMap">
		select
			*
		from
			local
		order by
			reg_date desc
	</select>
	
	<resultMap type="local" id="localMap">
		<id column="no" property="no"></id>
		<result column="category_no" property="categoryNo" />
		<result column="writer" property="writer" />
		<result column="title" property="title" />
		<result column="reg_date" property="regDate" />
	</resultMap>
	
	<select id="selectAdminTogetherList" resultMap="togetherMap">
		select
			*
		from
			together
		order by
			reg_date desc
	</select>
	
	<resultMap type="together" id="togetherMap">
		<id column="no" property="no"/>
		<result column="category_no" property="categoryNo"/>
		<result column="writer" property="writer"/>
		<result column="title" property="title"/>
		<result column="status" property="status"/>
		<result column="reg_date" property="regDate"/>
	</resultMap>
	
	<select id="selectAdminBoardReport" resultMap="adminMap">
		select
			*
		from
			board_report
		order by
			reg_date desc
	</select>

	
	<select id="selectAdminUserReport" resultMap="adminMap">
		select
			*
		from
			user_report
		order by
			reg_date desc
	</select>

</mapper>